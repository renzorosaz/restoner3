import 'package:flutter/material.dart';

class SesionFbPage extends StatelessWidget {
  const SesionFbPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('SesionFB'),
    );
  }
}